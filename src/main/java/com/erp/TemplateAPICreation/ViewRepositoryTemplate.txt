package com.erp.#FormHeading#.Repository;

import java.util.List;

import javax.transaction.Transactional;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import com.erp.#FormHeading#.Model.C#FormHeading#ViewModel;


public interface I#FormHeading#ViewRepository extends JpaRepository<C#FormHeading#ViewModel, Integer> {

	@Query(value = "FROM C#FormHeading#ViewModel model where model.is_delete = 0 and model.company_id = ?1 order by model.Trans_id Desc")
	Page<C#FormHeading#ViewModel> FnShowAllActiveRecords(Pageable pageable, int company_id);

	@Query(value = "FROM C#FormHeading#ViewModel model where model.is_delete =0 and model.Trans_id = ?1 and model.company_id = ?2 order by model.Trans_id Desc")
	Page<C#FormHeading#ViewModel> FnShowParticularRecord(int Trans_id, Pageable pageable, int company_id);
	

}